apiVersion: v1
kind: Service
metadata:
  labels:
    app: task-3
  name: task-3
spec:
  ports:
  - name: http
    port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    app: task-3
  type: LoadBalancer
